<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KhaataBook</title>
    <style>
        /* Basic Styles */
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        body {
            width: 100%;
            height: 100%;
            overflow-x: hidden;
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            font-family: Arial, sans-serif;
            color: #fff;
            scroll-behavior: smooth;
        }
        header {
            width: 100%;
            height: 100px;
            background: rgba(0, 0, 0, 0.5);
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
        }
        .mai {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
            animation: fadeIn 1s ease-in-out;
        }
        .mai h4 {
            font-size: 24px;
            font-weight: bold;
        }
        .left {
            display: flex;
            gap: 20px;
        }
        .left a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            position: relative;
            transition: color 0.3s ease-in-out;
        }
        .left a:hover {
            color: #ffda79;
        }
        .main {
            width: 100%;
            margin-top: 120px;
            padding: 20px;
            animation: fadeIn 1.5s ease-in-out;
        }
        .hissab p {
            background: rgba(255, 255, 255, 0.1);
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
            transition: background 0.3s ease-in-out;
        }
        .hissab p:hover {
            background: rgba(255, 255, 255, 0.2);
        }
        .hissab a {
            display: inline-block;
            margin-right: 10px;
            color: #ffda79;
            text-decoration: none;
            transition: color 0.3s ease-in-out;
        }
        .hissab a:hover {
            color: #fff;
        }
        h2 {
            font-size: 28px;
            margin-bottom: 20px;
            animation: fadeInUp 1.2s ease-in-out;
        }

        /* Animations */
        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }
        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <header>
        <div class="mai">
            <h4>KhaataBook</h4>
            <div class="left">
                <a class="ho" href="/">Home</a>
                <a href="/create">Create new hissab by date</a>
                <a href="/usercreation">Create new hissab manually</a>
                <a href="/contactus">Contact us</a>
            </div>
        </div>
    </header>
    <div class="main">
        <div>
            <h2>All Hissab Khitab Created by Date</h2>
            <div class="hissab">
                <% if(files.length > 0) { %>
                    <% files.forEach(function(file) { %>
                        <p><%= file %></p>
                        <a href="/open/<%= file %>">Open</a>
                        <a href="/edit/<%= file %>">Edit</a>
                        <a href="/delete/<%= file %>">Delete</a>
                    <% }) %>
                <% } else { %>
                    <h2>Nothing to show</h2>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
